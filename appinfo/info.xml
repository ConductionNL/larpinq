<?xml version="1.0"?>
<info xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://apps.nextcloud.com/schema/apps/info.xsd">
    <id>larpingapp</id>
    <name lang="en">Larping</name>
    <name lang="nl">Larping</name>
    <summary lang="en">Character, event and rules management for live-action role-play (LARP) groups</summary>
    <summary lang="nl">Karakter-, event- en regelbeheer voor live-action roleplay (LARP)-groepen</summary>
    <description lang="en"><![CDATA[Larping is a management tool for LARP (live-action role-play) organisations, built on Nextcloud and OpenRegister.

**Requires:** [OpenRegister](https://apps.nextcloud.com/apps/openregister) (install from the Nextcloud App Store).

Larping covers:

- Settings (worlds/campaigns) that scope characters, events and rules, archived rather than deleted when a campaign ends
- Character sheets with background stories and a game-master approval workflow
- Players linked to Nextcloud contacts — the person behind the character
- Game mechanics as typed records: abilities, skills, items, conditions and effects, with a deterministic stat-calculation engine
- Events with a participant roster, Nextcloud Calendar scheduling, and location/sign-up sidebar tabs
- Post-event XP awards with a full audit trail and server-enforced prerequisite checks (with GM override)
- Printable PDF character sheets and per-event GM run-sheets (cast lists), rendered via DocuDesk
- Group-restricted access: app visibility is limited to an enrolled `larpers` Nextcloud group

Create a [bug report](https://codeberg.org/Conduction/larpingapp/issues)
Create a [feature request](https://codeberg.org/Conduction/larpingapp/issues)

**Support:** For support, contact support@conduction.nl. For a Service Level Agreement (SLA), contact sales@conduction.nl.
    ]]></description>
    <description lang="nl"><![CDATA[Larping is een beheertool voor LARP-organisaties (live-action roleplay), gebouwd op Nextcloud en OpenRegister.

**Vereist:** [OpenRegister](https://apps.nextcloud.com/apps/openregister) (installeren via de Nextcloud App Store).

Larping biedt:

- Settings (werelden/campagnes) die karakters, events en regels afbakenen, en die je archiveert in plaats van verwijdert als een campagne afloopt
- Karakterbladen met achtergrondverhaal en een goedkeuringsworkflow voor de spelleider
- Spelers gekoppeld aan Nextcloud-contacten — de speler achter het karakter
- Spelmechanica als getypeerde records: abilities, skills, items, conditions en effects, met een deterministische stat-berekeningsmotor
- Events met een deelnemerslijst, planning via Nextcloud Agenda, en zijbalktabbladen voor locatie en aanmelden
- XP-toekenningen na afloop van een event, met volledige audit trail en server-side gecontroleerde vereisten (met GM-override)
- Afdrukbare PDF-karakterbladen en per-event GM-castlijsten, gerenderd via DocuDesk
- Toegang beperkt tot een aangemelde `larpers`-Nextcloud-groep

Maak een [bugmelding](https://codeberg.org/Conduction/larpingapp/issues)
Maak een [featureverzoek](https://codeberg.org/Conduction/larpingapp/issues)

**Support:** neem voor support contact op met support@conduction.nl. Voor een Service Level Agreement (SLA), neem contact op met sales@conduction.nl.
    ]]></description>
    <version>0.1.27-unstable.20260820112516</version>
    <licence>EUPL-1.2</licence>
    <author mail="info@conduction.nl" homepage="https://www.conduction.nl/">Conduction</author>
	<namespace>LarpingApp</namespace>
	<category>organization</category>
    <website>https://codeberg.org/Conduction/larpingapp</website>
    <bugs>https://codeberg.org/Conduction/larpingapp/issues</bugs>
    <repository type="git">https://codeberg.org/Conduction/larpingapp.git</repository>
    <screenshot>https://codeberg.org/Conduction/larpingapp/raw/branch/main/img/screenshot-dashboard.png</screenshot>
    <screenshot>https://codeberg.org/Conduction/larpingapp/raw/branch/main/img/screenshot-characters.png</screenshot>
    <screenshot>https://codeberg.org/Conduction/larpingapp/raw/branch/main/img/screenshot-events.png</screenshot>
    <dependencies>
        <!--
        App dependency: openregister. Recorded as prose, not as an element. The
        App Store schema (info.xsd) has no <app> child under <dependencies>, so
        shipping one fails App Store validation; Nextcloud's DependencyAnalyzer
        does not read it either (it handles only php, database, command, lib,
        os, architecture and the server version), so the element never enforced
        anything at install time. src/manifest.json is where the requirement is
        declared machine-readably.
        -->
        <php min-version="8.3" min-int-size="64"/>
        <database min-version="10">pgsql</database>
        <database>sqlite</database>
        <database min-version="8.0">mysql</database>
        <!--
        min-version is 32, NOT 28. Two independent reasons, both current:

        1. FLEET POLICY. The fleet floor is Nextcloud 32, which is the version
           that guarantees the PHP 8.3 baseline this app already declares below.
           Declaring <php min-version="8.3"/> while advertising NC 28 promises a
           combination we do not build, test or support.

        2. THE openregister APP DEPENDENCY NOTED ABOVE. An app's min-version
           must be >= the max of every app dependency's own floor
           (openconnector#1172/#1173). Every LarpingApp entity — characters,
           items, skills, effects, events — is an OpenRegister object, so where
           openregister cannot install this app is non-functional, not merely
           degraded. When openregister's floor was briefly 32 (its
           lib/ContextChat/ContentProvider.php implements
           OCP\ContextChat\IContentProvider, absent from core before NC 32), an
           NC 31 CI leg failed with "App 'Open Register' cannot be installed
           because it is not compatible with this version of the server" — and
           because the shared workflow emits that only as a ##[warning], the run
           carried on and died ~70s later on missing tables, which reads like a
           migration fault and sends you to entirely the wrong file.

           openregister's floor is 28 again at the time of writing
           (openregister#2380, after openregister#2372 removed the eager
           ContextChat reference), so reason 2 is not what forces 32 TODAY —
           reason 1 is. Do not "correct" this back down to match openregister's
           current floor: min-version is a >= bound on dependencies, not an
           equality, and this app has its own PHP floor to honour.

        CI matches this: nextcloud-test-refs in .github/workflows/code-quality.yml
        is ["stable32"] only, so no leg tests a version below the declared floor.
        Check that list before changing this value — PR #282/#284 ping-ponged
        because a 32 floor makes `occ app:enable` refuse on a stable31 leg, and
        the e2e seed then fails with "is not installed or enabled".
        -->
        <nextcloud min-version="32" max-version="34"/>
    </dependencies>
    <!-- Restrict app visibility to the 'larpers' group so that only enrolled GMs and
         players see the navigation entry and can probe endpoints. Admins may add users
         to this group via the Nextcloud admin panel. Closes #216.

         DUAL-GROUP REQUIREMENT: Game Masters need to be in BOTH the built-in 'admin'
         Nextcloud group AND the 'larpers' group. The 'larpers' group controls app
         visibility; admin privileges gate GM-only endpoints (e.g. downloadPdf).
         Regular players only need the 'larpers' group.

         The restriction is configured per instance, not declared here. The App
         Store schema (info.xsd) has no <groups> element, and Nextcloud does not
         read one either: OC_App::listAllApps() overwrites the parsed value with
         the app's `enabled` appconfig entry on every read, so a declaration in
         this file restricted nothing. Use the standard app-level group
         restriction instead: `occ app:enable larpingapp` with the groups flag
         (e.g. `groups larpers`), or the group selector on the app's entry in
         the Apps admin page. (XML comments must not contain a double hyphen,
         so the flag is not written with its leading dashes here.) -->

	<repair-steps>
		<post-migration>
			<step>OCA\LarpingApp\Repair\InitializeRegister</step>
		</post-migration>
		<install>
			<step>OCA\LarpingApp\Repair\InitializeRegister</step>
		</install>
	</repair-steps>

	<settings>
		<admin>OCA\LarpingApp\Settings\LarpingAppAdmin</admin>
		<admin-section>OCA\LarpingApp\Sections\LarpingAppAdmin</admin-section>
	</settings>

    <navigations>
        <navigation>
            <id>larpingapp</id>
            <name>Larping</name>
            <route>larpingapp.dashboard.page</route>
            <icon>app.svg</icon>
        </navigation>
    </navigations>
</info>
