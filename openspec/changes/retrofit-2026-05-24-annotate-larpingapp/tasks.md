# Tasks

- [x] task-1: deep-link-registration#DEEP-001 — A `DeepLinkRegistrationListener` MUST be registered in Application.php (retroactive annotation)
- [x] task-2: deep-link-registration#DEEP-004 — The listener MUST only fire when OpenRegister dispatches DeepLinkRegistrationEvent (retroactive annotation)
- [x] task-3: deep-link-registration#DEEP-005 — The listener MUST gracefully handle OpenRegister not being installed (retroactive annotation)
- [x] task-4: register-config-json#REG-004 — Application.boot() MUST call SettingsService.loadSettings() (retroactive annotation)
- [x] task-5: register-config-json#REG-006 — Import MUST be skipped silently if OpenRegister is not installed (retroactive annotation)
- [x] task-6: deep-link-registration#DEEP-002 — The listener MUST register URL templates for all 8 object types (retroactive annotation)
- [x] task-7: deep-link-registration#DEEP-003 — URL pattern MUST be `/apps/larpingapp/#/{type}/{uuid}` (retroactive annotation)
- [x] task-8: deep-link-registration#DEEP-006 — Characters MUST link to `/apps/larpingapp/#/characters/{uuid}` (retroactive annotation)
- [x] task-9: deep-link-registration#DEEP-007 — Players MUST link to `/apps/larpingapp/#/players/{uuid}` (retroactive annotation)
- [x] task-10: deep-link-registration#DEEP-008 — Abilities MUST link to `/apps/larpingapp/#/abilities/{uuid}` (retroactive annotation)
- [x] task-11: deep-link-registration#DEEP-009 — Skills MUST link to `/apps/larpingapp/#/skills/{uuid}` (retroactive annotation)
- [x] task-12: deep-link-registration#DEEP-010 — Items MUST link to `/apps/larpingapp/#/items/{uuid}` (retroactive annotation)
- [x] task-13: deep-link-registration#DEEP-011 — Conditions MUST link to `/apps/larpingapp/#/conditions/{uuid}` (retroactive annotation)
- [x] task-14: deep-link-registration#DEEP-012 — Effects MUST link to `/apps/larpingapp/#/effects/{uuid}` (retroactive annotation)
- [x] task-15: deep-link-registration#DEEP-013 — Events MUST link to `/apps/larpingapp/#/events/{uuid}` (retroactive annotation)
- [x] task-16: admin-settings#SET-002 — The admin section MUST use the LarpingApp icon (`app-dark.svg`) (retroactive annotation)
- [x] task-17: admin-settings#SET-001 — LarpingApp MUST have a dedicated admin section in the Nextcloud admin settings panel (retroactive annotation)
- [x] task-18: admin-settings#SET-005 — The admin section MUST have priority 55 in the settings panel ordering (retroactive annotation)
- [x] task-19: admin-settings#SET-004 — Settings MUST be rendered using a Vue component (`Settings.vue`) via the `settings` entry point (retroactive annotation)
- [x] task-20: admin-settings#SET-040 — The system MUST retrieve all settings via `GET /api/settings` (retroactive annotation)
- [x] task-21: admin-settings#SET-042 — Settings response MUST include: `objectTypes` array, `openRegisters` boolean, `isAdmin` boolean, and `configuration` object (retroactive annotation)
- [x] task-22: admin-settings#SET-020 — The system MUST check if the OpenRegister app is installed via `IAppManager::getInstalledApps()` (retroactive annotation)
- [x] task-23: admin-settings#SET-043 — The `configuration` object MUST contain the CONFIG_KEYS values (register, per-type schemas) (retroactive annotation)
- [x] task-24: admin-settings#SET-041 — The system MUST update settings via `POST /api/settings` (retroactive annotation)
- [x] task-25: register-config-json#REG-012 — SettingsController.reimport() MUST re-import configuration on POST /api/settings/reimport (retroactive annotation)
- [x] task-26: admin-settings#SET-053 — `SettingsController.reimport()` (POST /api/settings/reimport) MUST be admin-only and trigger forced re-import of configuration (retroactive annotation)
- [x] task-27: register-config-json#REG-005 — SettingsLoadService MUST delegate to ConfigurationService.importFromApp() (retroactive annotation)
- [x] task-28: admin-settings#SET-060 — `SettingsLoadService` MUST load configuration from the bundled JSON file (retroactive annotation)
- [x] task-29: admin-settings#SET-061 — Import MUST create registers and schemas in OpenRegister based on the JSON definition (retroactive annotation)
- [x] task-30: admin-settings#SET-063 — Import MUST support a `force` flag to re-import even when configuration already exists (retroactive annotation)
- [x] task-31: register-config-json#REG-010 — SettingsLoadService MUST update IAppConfig with imported register/schema IDs (retroactive annotation)
- [x] task-32: admin-settings#SET-062 — Import MUST persist the created register and schema IDs in IAppConfig (retroactive annotation)
- [x] task-33: register-config-json#REG-001 — A `larpingapp_register.json` file MUST exist at `lib/Settings/` (retroactive annotation)
- [x] task-34: register-config-json#REG-007 — ConfigFileLoaderService MUST load and parse the JSON file (retroactive annotation)
- [x] task-35: register-config-json#REG-008 — ConfigFileLoaderService MUST throw RuntimeException if file not found (retroactive annotation)
- [x] task-36: register-config-json#REG-009 — ConfigFileLoaderService MUST ensure x-openregister sourceType is set (retroactive annotation)
- [x] task-37: register-config-json#REG-011 — SettingsMapBuilder MUST build slug-to-ID mapping from import result (retroactive annotation)
- [x] task-38: object-service#OBJ-010 — `getOpenRegisterService()` MUST check if OpenRegister app is installed via `IAppManager::getInstalledApps()` (retroactive annotation)
- [x] task-39: object-service#OBJ-011 — `getOpenRegisterService()` MUST resolve `OCA\OpenRegister\Service\ObjectService` from the DI container (retroactive annotation)
- [x] task-40: object-service#OBJ-012 — `getOpenRegisterService()` MUST cache the resolved service in `$openRegisterService` private property (retroactive annotation)
- [x] task-41: object-service#OBJ-013 — `getOpenRegisterService()` MUST return cached instance on subsequent calls (retroactive annotation)
- [x] task-42: object-service#OBJ-014 — `getOpenRegisterService()` MUST throw an Exception if OpenRegister is not installed (retroactive annotation)
- [x] task-43: object-service#OBJ-001 — `getMapper()` MUST read `{objectType}_register` from IAppConfig using `getValueString()` (retroactive annotation)
- [x] task-44: object-service#OBJ-002 — `getMapper()` MUST read `{objectType}_schema` from IAppConfig using `getValueString()` (retroactive annotation)
- [x] task-45: object-service#OBJ-003 — `getMapper()` MUST convert the objectType to lowercase before config key lookup (retroactive annotation)
- [x] task-46: object-service#OBJ-004 — `getMapper()` MUST obtain the mapper from OpenRegister's ObjectService via `$openRegister->getMapper($register, $schema)` (retroactive annotation)
- [x] task-47: object-service#OBJ-005 — `getMapper()` MUST throw an Exception if register is not configured (empty string) (retroactive annotation)
- [x] task-48: object-service#OBJ-006 — `getMapper()` MUST throw an Exception if schema is not configured (empty string) (retroactive annotation)
- [x] task-49: object-service#OBJ-007 — The appName MUST be hardcoded to 'larpingapp' for IAppConfig lookups (retroactive annotation)
- [x] task-50: object-service#OBJ-020 — `getObjects()` MUST accept: objectType, limit, offset, filters, sort, search parameters (retroactive annotation)
- [x] task-51: object-service#OBJ-021 — `getObjects()` MUST pass parameters through to the mapper's `findAll()` method (retroactive annotation)
- [x] task-52: object-service#OBJ-022 — `getObjects()` MUST convert all returned objects to arrays via `toArray()` (retroactive annotation)
- [x] task-53: object-service#OBJ-024 — Default parameter values MUST be: limit=null, offset=null, filters=[], sort=[], search=null (retroactive annotation)
- [x] task-54: object-service#OBJ-030 — `getObject()` MUST accept objectType (string) and id (string) (retroactive annotation)
- [x] task-55: object-service#OBJ-031 — `getObject()` MUST clean URI-format IDs by extracting the last path segment (retroactive annotation)
- [x] task-56: object-service#OBJ-032 — URI cleaning MUST only activate when `filter_var($id, FILTER_VALIDATE_URL)` returns a valid URL (retroactive annotation)
- [x] task-57: object-service#OBJ-033 — `getObject()` MUST call the mapper's `find()` method with the cleaned ID (retroactive annotation)
- [x] task-58: object-service#OBJ-034 — `getObject()` MUST convert the result to an array via `toArray()` (retroactive annotation)
- [x] task-59: object-service#OBJ-023 — `toArray()` MUST handle objects with `jsonSerialize()`, plain arrays, and other types (retroactive annotation)
- [x] task-60: object-service#OBJ-040 — `toArray()` MUST call `jsonSerialize()` on objects that have this method (retroactive annotation)
- [x] task-61: object-service#OBJ-041 — `toArray()` MUST return arrays unchanged (retroactive annotation)
- [x] task-62: object-service#OBJ-042 — `toArray()` MUST cast other types to array via `(array)` (retroactive annotation)
- [x] task-63: object-service#OBJ-043 — `toArray()` MUST accept `mixed` type parameter (retroactive annotation)
- [x] task-64: character-management#CHAR-070 — `CharacterService.calculateAllCharacters()` MUST retrieve all characters and recalculate each one's stats (retroactive annotation)
- [x] task-65: character-management#CHAR-071 — `calculateAllCharacters()` MUST return an array of updated character arrays with recalculated stats (retroactive annotation)
- [x] task-66: character-management#CHAR-073 — `calculateAllCharacters()` uses `RegisterObjectFetcher.getObjects('character')` to fetch all characters (retroactive annotation)
- [x] task-67: character-management#CHAR-010 — Created/updated characters MUST have stats recalculated via `CharacterService.calculateCharacter()` (retroactive annotation)
- [x] task-68: character-management#CHAR-050 — On character create/update, `CharacterService.calculateCharacter()` MUST be invoked (retroactive annotation)
- [x] task-69: character-management#CHAR-052 — Effects from the character's skills MUST be applied to ability scores via `applyEntityEffects()` (retroactive annotation)
- [x] task-70: events-players#EVT-020 — `CharacterService.calculateCharacter()` MUST call `applyEntityEffects()` with property 'events' and the events lookup (retroactive annotation)
- [x] task-71: game-mechanics#MECH-007 — Ability base value MUST be used by `CharacterService.initializeAbilityScores()` to set starting values for all character ability scores (retroactive annotation)
- [x] task-72: rpg-system#ABIL-009 — `CharacterService.initializeAbilityScores()` MUST use all abilities to create the starting stat map with base values (retroactive annotation)
- [x] task-73: game-mechanics#MECH-063 — The stat engine MUST handle null or missing effect IDs without throwing errors — `applyEffects()` skips null effectIds (retroactive annotation)
- [x] task-74: game-mechanics#MECH-024 — Effect MUST support targeting multiple abilities via the `abilities[]` array (retroactive annotation)
- [x] task-75: game-mechanics#MECH-025 — Effect MUST support legacy `stat_id` field for single-ability targeting (retroactive annotation)
- [x] task-76: game-mechanics#MECH-026 — `calculateEffect()` MUST call `collectEffectAbilities()` to gather all targeted abilities from both `abilities[]` and `stat_id` (retroactive annotation)
- [x] task-77: game-mechanics#MECH-027 — `applyModifierToAbility()` MUST add modifier for "positive" and subtract for "negative" modification (retroactive annotation)
- [x] task-78: game-mechanics#MECH-028 — `applyModifierToAbility()` MUST append an audit entry with `{type, effect, old, new}` for each modification (retroactive annotation)
- [x] task-79: game-mechanics#MECH-062 — Deleting an ability MUST NOT crash the stat engine — null ability IDs are gracefully skipped by `applyModifierToAbility` (retroactive annotation)
- [x] task-80: pdf-export#PDF-001 — `CharactersController.downloadPdf()` MUST generate a PDF from a character and a selected template (retroactive annotation)
- [x] task-81: pdf-export#PDF-002 — The controller MUST resolve DocuDesk's `PdfService` via `$this->container->get('OCA\DocuDesk\Service\PdfService')` (retroactive annotation)
- [x] task-82: pdf-export#PDF-003 — The controller MUST resolve DocuDesk's `TemplateService` via `$this->container->get('OCA\DocuDesk\Service\TemplateService')` (retroactive annotation)
- [x] task-83: pdf-export#PDF-004 — Template content MUST be fetched via `TemplateService::getTemplate($template)` (retroactive annotation)
- [x] task-84: pdf-export#PDF-005 — PDF MUST be rendered via `PdfService::renderPdf()` with template content, data context, and options (retroactive annotation)
- [x] task-85: pdf-export#PDF-006 — Data context MUST include `character` (full character data) and `template` (template metadata) (retroactive annotation)
- [x] task-86: pdf-export#PDF-007 — PDF options MUST include `format` (default 'A4') and `orientation` (default 'P') from template metadata (retroactive annotation)
- [x] task-87: pdf-export#PDF-008 — The PDF MUST be returned as a `DataDownloadResponse` with `application/pdf` MIME type (retroactive annotation)
- [x] task-88: pdf-export#PDF-009 — The filename MUST be `{characterName}_character_sheet.pdf` (retroactive annotation)
- [x] task-89: pdf-export#PDF-020 — `downloadPdf()` MUST check `IAppManager::isEnabledForUser('docudesk')` before attempting PDF generation (retroactive annotation)
- [x] task-90: pdf-export#PDF-021 — If DocuDesk is not installed/enabled, the controller MUST return a 424 (Failed Dependency) JSONResponse (retroactive annotation)
- [x] task-91: pdf-export#PDF-022 — The 424 response MUST include the DocuDesk-required error message (retroactive annotation)
- [x] task-92: pdf-export#PDF-030 — When the character ID does not exist, the controller MUST return a 404 JSONResponse with Character-not-found error (retroactive annotation)
- [x] task-93: pdf-export#PDF-031 — When the template ID does not exist, the controller MUST return a 404 JSONResponse with Template-not-found error (retroactive annotation)
- [x] task-94: pdf-export#PDF-032 — When PDF generation fails, the controller MUST return a 500 JSONResponse with PDF-generation-failed error (retroactive annotation)
- [x] task-95: pdf-export#PDF-033 — Character retrieval MUST use `RegisterObjectFetcher::getObject('character', $id)` (retroactive annotation)
- [x] task-96: pdf-export#PDF-034 — Template retrieval errors MUST be caught and returned as 404 (retroactive annotation)
- [x] task-97: pdf-export#PDF-035 — The method signature MUST declare return type `DataDownloadResponse|JSONResponse` (retroactive annotation)
- [x] task-98: dashboard#DASH-001 — DashboardController MUST serve a TemplateResponse for the root route `/` (retroactive annotation)
- [x] task-99: dashboard#DASH-002 — The template MUST render the `index` template from the `larpingapp` app (retroactive annotation)
- [x] task-100: dashboard#DASH-003 — The route MUST be accessible without admin rights (`@NoAdminRequired`) (retroactive annotation)
- [x] task-101: dashboard#DASH-004 — The route MUST not require CSRF validation (`@NoCSRFRequired`) (retroactive annotation)
