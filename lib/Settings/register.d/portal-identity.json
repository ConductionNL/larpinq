{
  "components": {
    "schemas": {
      "character": {
        "version": "1.4.0",
        "properties": {
          "ownerRef": {
            "type": "string",
            "format": "uuid",
            "$ref": "player",
            "title": "Owner",
            "description": "UUID reference to the player object — the portal-subject scoping key; distinct from ownerUid which is the Nextcloud user id"
          }
        }
      }
    }
  }
}
